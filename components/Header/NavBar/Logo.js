import { Box, Text } from '@chakra-ui/react';

const Logo = (props) => {
  return(
    // Logo
    <Box {...props}>
      <Text fontSize="lg" fontWeight="bold">
        Archerynted
      </Text>
    </Box>
  )
}

export default Logo;
